= link_to item_path (item) do
  = image_tag(item.images[0].image.url, class: "search-result__image") if item.images[0]
  %h4.search-result__introduction
    = item.name
  %h4.search-result__price
    ¥
    = item.price
  %p (税込)
